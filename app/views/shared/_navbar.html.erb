<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <%= link_to 'Home', index_path, class: "nav-link" %>
                </li>
            

                <% if current_player %>
                    <%= link_to 'Logout', destroy_player_session_path, method: :delete, class: "nav-link" %>
                    <%= link_to 'Edit_player', edit_player_registration_path, class: "nav-link" %>

                <% else %>
                    <%= link_to 'Register', new_player_registration_path, class: "nav-link" %>
                    <%= link_to ' Login', edit_player_registration_path, class: "nav-link" %>
                <% end %>
            </ul>
        
        </div>
    </div>
</nav>